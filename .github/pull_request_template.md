## Summary

<!-- What was wrong, and what does this change? Lead with the defect or the gap, not the diff. -->

## Linked issues

<!--
Repeat the keyword per issue. GitHub only auto-closes the FIRST issue in a
space-separated list:

  Closes #101, Closes #102   ✅ both close
  Closes #101 #102           ❌ only #101 closes
  Closes #101 and #102       ❌ only #101 closes

After merge, confirm every referenced issue actually closed.
-->

Closes #

## Changes

| Issue | Change |
|---|---|
|  |  |

## Checks run locally

<!--
Paste what you actually ran and what it returned. `make test` covers
tune/operate/scope/history; the root package's E2E suite needs a separate
`swift test` at the repo root, and the GUI bundle needs `make build-app`.
-->

| Command | Result |
|---|---|
| `make test` |  |
| `swift test` (root) |  |

## Acceptance criteria

<!-- Copy the criteria from the linked issue and tick what this PR actually satisfies. -->

- [ ]

## Known limitations

<!--
Anything deliberately not done, any deviation from what the issue asked for
and why, and any criterion left unmet. A criterion that cannot be verified
here belongs in this section, not silently ticked above.
-->

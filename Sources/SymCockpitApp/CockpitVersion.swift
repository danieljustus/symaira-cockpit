import SymCockpitVersion

/// Compatibility name used by the GUI's existing views and app delegate.
/// The release value itself is owned by the shared library target.
typealias CockpitAppVersion = CockpitVersion
